import Alert from '@mui/material/Alert';
import { CodeEdit, InfoPage } from '../../components/index.jsx';

<InfoPage>
	[https://webdoky.org](https://webdoky.org/uk/docs/Web/JavaScript/Closures/#praktychni-zamykannia)
	[https://doka.guide](https://doka.guide/js/recursion/)
</InfoPage>

# Замикання і Рекурсія

## Замикання

Для прикладу – наступний код:

<CodeEdit text={`
function makeFunc() {
  const name = "ВебДоки";
  function displayName() {
    console.log(name);
  }
  return displayName;
}

const myFunc = makeFunc();
myFunc();
`}/>

На перший погляд, може здатись неінтуїтивним, що цей код працює. В деяких мовах програмування локальні змінні всередині функції існують лише протягом виконання такої функції. Оскільки ```makeFunc()``` завершила виконання, можна було б очікувати, що змінна ```name``` більше не доступна. Проте, оскільки код і далі працює як очікувалося, в JavaScript це очевидно не так.

Причина цього в тому, що функції в JavaScript утворюють замикання. Замикання – це поєднання функції й лексичного оточення, всередині якого ця функція оголошена. Таке оточення складається з усіх змінних, що були в поточній області видимості, коли створювалось замикання. В конкретному випадку ```myFunc``` є посиланням на примірник функції ```displayName```, що створюється, коли запускається ```makeFunc```. Примірник ```displayName``` зберігає посилання на своє лексичне оточення, всередині якого існує змінна ```name```. Через це, коли закликається ```myFunc```, то змінна name залишається доступною для використання, і в ```console.log``` передається "ВебДоки".

Ось дещо цікавіший приклад – функція ```makeAdder```:

<CodeEdit text={`
function makeAdder(x) {
  return function (y) {
    return x + y;
  };
}

const add5 = makeAdder(5);
const add10 = makeAdder(10);

console.log(add5(2)); // 7
console.log(add10(2)); // 12
`}/>

У цьому прикладі визначена функція ```makeAdder(x)```, що приймає єдиний аргумент – ```x```, і повертає нову функцію. Повернена функція приймає єдиний аргумент – ```y```, і повертає суму ```x``` та ```y```.

По суті ```makeAdder``` є фабрикою функцій. Вона породжує функції, що можуть додати до свого аргументу певне значення. У прикладі вище фабрика функцій породжує дві нові функції: одну, що додає до свого аргументу п'ять, і одну, що додає 10.

І ```add5```, і ```add10``` – утворюють замикання. Вони поділяють одне оголошення тіла функції, але зберігають різні лексичні оточення. В лексичному оточенні ```add5``` ```x``` – це 5, а в лексичному оточенні ```add10``` ```x``` – це 10.

#### Замикання на практиці:

<CodeEdit text={`
function makeSizer(size) {
  return function () {
    document.body.style.fontSize = "size px"; size - аргумент
  };
}

const size12 = makeSizer(12);
const size14 = makeSizer(14);
const size16 = makeSizer(16);
`}/>

Наразі ```size12```, ```size14``` і ```size16``` – функції, що змінюють розмір тексту тіла документа на 12, 14 і 16 пікселів відповідно. Їх можна прикріпити до кнопок (в цьому випадку – гіперпосилань), як показано в прикладі коду нижче.

<CodeEdit text={`
document.getElementById("size-12").onclick = size12;
document.getElementById("size-14").onclick = size14;
document.getElementById("size-16").onclick = size16;

<button id="size-12">12</button>
<button id="size-14">14</button>
<button id="size-16">16</button>
`}/>

## Рекурсия

У програмуванні під рекурсією найчастіше розуміють функцію, що викликає сама себе.

При вирішенні деяких завдань ми можемо виявити, що рішення можна розбити на кілька простих дій і простіший варіант того завдання.

Наприклад, при зведенні числа в ступінь ми беремо число, множимо його кілька разів. Цю операцію можна представити у вигляді:

<CodeEdit text={`
// 2^5 = 2 * 2 * 2 * 2 * 2
//
// 1 крок: 2
// 2 крок: 2 * 2
// 3 крок: 2 * 2 * 2
// 4 крок: 2 * 2 * 2 * 2
// 5 крок: 2 * 2 * 2 * 2 * 2
//
// Який за рахунком крок -
// стільки і множень.
`}/>

У рекурсії 2 складові: операції, що повторюються, і базовий випадок.


#### Операції, що повторюються

У прикладі зі зведенням у ступінь операція що повторюється це множення.

Такі операції можуть бути складними і включати кілька підзавдань.

#### Базовий випадок
Друга важлива частина рекурсії – це базовий випадок.
💡
Базовий випадок - це умова, при виконанні якої рекурсія закінчується і функція більше не викликає себе.

Наприклад, при зведенні в ступінь базовий випадок настає, коли значення ступеня стає рівним шуканому.

Без базового випадку будь-яка рекурсивна функція піде в нескінченне виконання, бо викликатиме себе без кінця.

#### Приклад: Факторіал за допомогою рекурсії

<CodeEdit text={`
function factorial(n) {
  // Якщо ми намагаємося знайти факторіал 1,
  // Повертаємо 1 - це базовий випадок.

  if (n <= 1) {
    return 1
  }

  // В інших випадках
  // Повертаємо множення n
  // на факторіал попереднього числа -
  // таким чином ми від n дійдемо до 1,
  // Перебравши кожне число.

  return n * factorial(n - 1)
}

console.log(factorial(5)) // 120
`}/>

#### Мінуси такої реалізації:

* Багато областей видимості (замикань), які є відносно вимогливими до пам'яті.
* Просто читати, але складніше налагоджувати, ніж цикл.

#### Плюси:

* Якщо ми часто рахуєм факторіали, ми можемо кешувати результати виконання, щоб не рахувати факторіали заново. Рекурсивна функція з кешем повертатиме порахований раніше результат відразу ж, цикл же рахуватиме заново.
* Неможливо вплинути на процес підрахунку ззовні, замикання не дають зовнішньому світу отримати доступ до змінних всередині.